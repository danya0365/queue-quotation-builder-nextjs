/**
 * Presentation Hooks
 * Custom hooks for presenter pattern following Clean Architecture
 */

export { useBuilderPresenter } from './useBuilderPresenter';
export { useQuotePresenter } from './useQuotePresenter';

